# Rust
/target
**/*.rs.bk
*.pdb

# Credenciais / config local
.env
.env.*
!.env.example

# Node / npm (empacotamento do CLI)
node_modules/
npm-debug.log*
*.tgz
.npm/

# Binários por plataforma (preenchidos pelo CI no release) — o launcher JS do
# pacote principal (npm/ado-cli/bin/) NÃO é ignorado.
/npm/platforms/*/bin/
dist/

# Editores / SO
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
